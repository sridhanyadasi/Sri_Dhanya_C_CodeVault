/*
Use constant as a variable name.
*/
#include<stdio.h>
void main()
{
    int constant = 10;   // constant is not a keyword
    printf("%d", constant);
}

/*
Output:
10

Reason:
constant is not a keyword, so it works correctly.
*/
