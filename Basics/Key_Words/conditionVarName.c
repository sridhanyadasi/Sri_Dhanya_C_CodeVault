/*
Use condition as a variable name.
*/
#include<stdio.h>
void main()
{
    int condition = 6;   // condition is a normal word
    printf("%d", condition);
}

/*
Output:
6

Reason:
condition is not a keyword, so it is allowed as a variable name.
*/
