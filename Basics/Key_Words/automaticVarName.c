/*
Use automatic as a variable name.
*/
#include<stdio.h>
void main()
{
    int automatic = 9;   // automatic is a normal identifier
    printf("%d", automatic);
}

/*
Output:
9

Reason:
automatic is not a keyword, so it can be used as a variable name.
*/
