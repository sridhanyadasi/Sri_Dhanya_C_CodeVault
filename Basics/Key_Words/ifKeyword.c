/*
Try to use if as a variable name.
*/
#include<stdio.h>
void main()
{
    int if = 10;   // if is a keyword
    printf("%d", if);
}

/*
Error:
Compilation Error

Reason:
if is a decision-making keyword and cannot be used as a variable name.
*/
